import styled from "styled-components";

const Container = styled.div`
  height: 90vh;
  display: flex;
  justify-content: left;
  align-items: auto;
  margin: 0 auto;
  border-radius: 24px;
  box-shadow: 0 0px 20px 0px rgba(0, 0, 0, 0.6);
  background: linear-gradient(45deg, #e48f45 0%, #fa9a00 100%);
`;

export default Container;
